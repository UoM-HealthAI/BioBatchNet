# Core dependencies for BioBatchNet
torch>=2.0.0
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.3.0
scipy>=1.10.0
tqdm>=4.65.0
pyyaml>=6.0
anndata>=0.10.0
scanpy>=1.9.0
matplotlib>=3.7.0
seaborn>=0.12.0
h5py>=3.8.0

# Optional dependencies
# Uncomment if needed:

# For batch effect evaluation
# scib>=1.1.0

# For experiment tracking
# wandb>=0.15.0

# For additional visualization
# plotly>=5.14.0

# Note: For CUDA support with PyTorch, install with:
# pip install torch --index-url https://download.pytorch.org/whl/cu118